{
  "configurations": [
    {
      "type": "node-terminal",
      "name": "docs:dev",
      "request": "launch",
      "command": "pnpm run docs:dev",
      "cwd": "${workspaceFolder}"
    },
    {
      "type": "node-terminal",
      "name": "docs:build",
      "request": "launch",
      "command": "pnpm run docs:build",
      "cwd": "${workspaceFolder}"
    }
  ]
}
